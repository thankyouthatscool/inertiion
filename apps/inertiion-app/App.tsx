import "react-native-gesture-handler";

import { Text, View } from "react-native";

import { AppRoot } from "@components";

export const App = () => {
  return (
    <View>
      <AppRoot />
    </View>
  );
};
